/**
 * @module rooms/asesoria (engine)
 * @description Stub de Etapa 2b del plan journey-salas-estandar:
 *   placeholder "en construccion". La sala real se construye siguiendo
 *   docs/specs/journey-salas-estandar/17-sala-asesoria.md.
 */
import type { RoomFactory } from '../world'
import { underConstruction } from './stub'

const buildAsesoria: RoomFactory = (ctx) => underConstruction(ctx)

export default buildAsesoria
