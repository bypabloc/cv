[pytest]
# rootdir = raiz del Lambda contact_form. Los tests corren con core/ en el
# path (lo agrega tests/conftest.py) y shared/ vendorizado en core/shared/.
minversion = 8.0
testpaths = tests
markers =
    unit: tests unitarios (rapidos, sin AWS real)
    integration: tests con AWS real (deployed)
