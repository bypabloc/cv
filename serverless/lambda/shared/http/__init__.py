"""Subpaquete `http`: Capa HTTP: CORS, responses, ip_extractor, turnstile, validators.

Subpaquete SIN barrel: este `__init__` NO re-exporta nada. Importar
SIEMPRE del modulo concreto (contrato `.claude/rules/lambda-shared-imports.md`):

    from shared.http.responses import json_response
    from shared.http.cors import resolve_origin
"""
