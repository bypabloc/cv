# Dependencias de desarrollo del Lambda tracking_pixel (runtime + testing).
# Incluye requirements.txt + las libs de test.

-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-mock>=3.12,<4.0
moto[dynamodb]>=5.0,<6.0
