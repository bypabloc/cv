# Dependencias del Lambda contact_form.
# SAM las instala en el zip de la funcion (sin Lambda Layers — cada Lambda
# declara lo suyo). boto3 viene preinstalado en el runtime de AWS Lambda,
# pero se pinnea para que dev local / tests usen la misma version.

aws-lambda-powertools[all]>=3.0.0,<4.0
httpx>=0.27.0,<1.0
pydantic[email]>=2.5,<3.0
pydantic-settings>=2.0,<3.0
boto3>=1.34.0,<2.0
