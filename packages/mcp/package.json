{
  "name": "@portfolio/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol (MCP) server for the portfolio — JSON-RPC handlers + 3 CV exploration tools shared across 6 Cloudflare Pages Functions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.9.3",
    "vite": "^7.3.3",
    "vitest": "^2.1.9"
  }
}
