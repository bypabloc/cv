"""bypass_token — keygen + mint del token de bypass de Turnstile (Ed25519).

Lado firmante (devtools) del bypass firmado: genera los pares Ed25519 por
env y emite tokens efimeros verificables por el backend
(`shared.crypto.bypass_token`). NUNCA prod.
"""
