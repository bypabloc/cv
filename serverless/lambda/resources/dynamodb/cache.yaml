# Esquema devtools — NO CloudFormation: esquema plano, sin funciones
# intrinsecas. devtools lo lee y emite las llamadas AWS CLI.
# DynamoDB CacheTable (cache key-value con TTL, sin Streams). devtools la
# provisiona con `aws dynamodb create-table` + `update-time-to-live` y
# publica name/arn en SSM.
kind: dynamodb-table
name: portfolio-cache-${stage}
billing_mode: PAY_PER_REQUEST
hash_key: { name: cache_key, type: S }
range_key: null
stream: null
point_in_time_recovery: false
encryption: true
ttl_attribute: expires_at
publishes_ssm:
  name: /portfolio/${stage}/dynamodb/cache/name
  arn: /portfolio/${stage}/dynamodb/cache/arn
tags: { Project: portfolio, ManagedBy: devtools }
