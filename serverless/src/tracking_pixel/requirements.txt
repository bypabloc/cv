# Dependencias del Lambda tracking_pixel.
# SAM las instala en el zip de la funcion (sin Lambda Layers). boto3 viene
# preinstalado en el runtime de AWS Lambda; se pinnea para parity local.

aws-lambda-powertools[all]>=3.0.0,<4.0
pydantic[email]>=2.5,<3.0
pydantic-settings>=2.0,<3.0
boto3>=1.34.0,<2.0
