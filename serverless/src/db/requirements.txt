# Dependencias del Lambda db (gestion del schema via Alembic).
# SAM las instala en el zip de la funcion (sin Lambda Layers). Corre Alembic
# dentro de AWS: SQLAlchemy (ORM, metadata) + alembic + psycopg (driver).
# boto3 viene preinstalado en el runtime; se pinnea para parity local.

aws-lambda-powertools[all]>=3.0.0,<4.0
boto3>=1.34.0,<2.0
psycopg[binary]>=3.2,<4.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
