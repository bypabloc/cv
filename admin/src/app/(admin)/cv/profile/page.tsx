"use client";

import { ProfileSection } from "@/features/cv-management";

/**
 * @page CvProfilePage
 * @description Edicion del profile singleton del CV (headline/summary
 *   BiLang, contactos, stats, niches).
 */
export default function CvProfilePage() {
	return <ProfileSection />;
}
