/**
 * @module rooms/ipasme (engine)
 * @description Stub de Etapa 1 del plan journey-salas-estandar: placeholder
 *   "en construccion". La sala real se construye en Etapa 2 siguiendo
 *   docs/specs/journey-salas-estandar/09-sala-ipasme.md.
 */
import type { RoomFactory } from '../world'
import { underConstruction } from './stub'

const buildIpasme: RoomFactory = (ctx) => underConstruction(ctx)

export default buildIpasme
