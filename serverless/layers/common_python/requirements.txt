# Lambda Layer compartido (Powertools v3 + httpx + pydantic)
# Compilado para arm64 Graviton2 / Python 3.13
# Build: sam build --use-container

aws-lambda-powertools[all]>=3.0.0,<4.0
httpx>=0.27.0,<1.0
pydantic[email]>=2.5.0,<3.0
pydantic-settings>=2.0,<3.0
bcrypt>=4.0,<5.0
