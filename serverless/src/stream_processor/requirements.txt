# Dependencias del Lambda stream_processor.
# SAM las instala en el zip de la funcion (sin Lambda Layers). Toca Neon
# via el ORM: psycopg (driver) + SQLAlchemy. boto3 viene preinstalado en el
# runtime; se pinnea para parity local. pydantic valida el evento sintetico
# {operation, action, data} (estandar lambda-controller).

aws-lambda-powertools[all]>=3.0.0,<4.0
boto3>=1.34.0,<2.0
psycopg[binary]>=3.2,<4.0
pydantic>=2.0,<3.0
sqlalchemy>=2.0,<3.0
