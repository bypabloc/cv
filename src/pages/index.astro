---
import Layout from "@/layouts/Layout.astro";

import { getUser } from "@/utils/db/getUser";
import Page from "@/components/Page.astro";

const user = await getUser("bypabloc");
---

<Layout title="Portafolio">
  <Page :user={user} />
</Layout>
